# Core data science libraries - updated for Python 3.11 compatibility
numpy==1.26.0
pandas==2.1.0
scikit-learn==1.3.2
scipy==1.11.3
librosa==0.10.1
soundfile==0.12.1
joblib==1.3.2

# Audio processing
pydub==0.25.1
audioread==3.0.0

# For handling different audio formats
ffmpeg-python==0.2.0

# For imbalanced data
imbalanced-learn==0.11.0

# For Streamlit app - updated versions
streamlit==1.28.1
plotly==5.17.0
matplotlib==3.8.0
seaborn==0.13.0

# Additional helpful packages
tqdm==4.66.1

# NumPy – Used for fast numerical computations and handling audio signal arrays.
# Pandas – Used to organize and manipulate structured data like datasets and feature tables.
# Scikit-learn – Provides machine learning algorithms to train and evaluate the stress detection model.
# SciPy – Offers scientific and signal-processing tools for analyzing audio signals.
# Librosa – Specialized library for audio analysis and extracting features like MFCC from speech.
# Soundfile – Reads and writes audio files efficiently, especially .wav files.
# Joblib – Saves and loads trained machine learning models for later use.
# PyDub – Used for basic audio editing like trimming, converting, or adjusting audio segments.
# Audioread – Backend library that helps load and decode different audio formats.
# FFmpeg-Python – Python interface for FFmpeg to process and convert audio/video files.
# Imbalanced-learn – Handles imbalanced datasets using techniques like oversampling or SMOTE.
# Streamlit – Builds a simple web interface where users can upload audio and see predictions.
# Plotly – Creates interactive visualizations for the web app.
# Matplotlib – Generates basic plots like waveforms, graphs, and training results.
# Seaborn – Creates advanced statistical visualizations such as confusion matrices.


